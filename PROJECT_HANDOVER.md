# 潮港城電商購物（eshopckc.com）電商優化專案 交接文件

潮港城電商購物（eshopckc.com）

電商優化專案 交接文件

文件版本 v1.7　｜　2026-08-30　｜　涵蓋 2026-07-15 ～ 08-30 全部已上線變更＋待實作規格

一、專案概要

本專案針對 eshopckc.com（WordPress + WooCommerce，WordPress.com 代管）進行全站購物流程優化、後台出貨AI助理擴充（黑貓宅急便整合），並自建完整的分潤（聯盟行銷）系統。所有變更集中於自訂主題 chao-gang-cheng，未修改任何外掛或核心檔案。除另有註明外，以下所有項目皆已部署至正式站並實測驗證通過。

二、已完成工作總覽（皆已上線驗證）

2.1 結帳頁（第一階段）

訪客結帳預設化（建立帳號預設不勾選）；綠界站內付 2.0 付款失敗改善：可行動錯誤文案＋「重新載入付款模組」重試按鈕＋「付款處理中請勿重新整理」提示＋完整後端 logging；付款方式依台灣偏好排序（信用卡→超商取貨付款→LINE Pay→ATM→超商代碼）附說明文字。

2.2 購物車頁

數量變更自動重算（免按更新鈕，0.6 秒 debounce，進度條同步）；總計區「預估運費」列（動態讀費率、達標顯示免運）；未達免運門檻時顯示湊免運加購推薦（最多 4 件、一鍵加入）；行動版吸底結帳列；繼續購物連結；SSL 信任標章＋配送政策連結；免運門檻改為單一來源（動態讀運送區域設定）。

2.3 商品頁

紅利折抵文案動態化（依商品價格 × 1:1 規則計算上限）；加價購限定冷凍食品／下酒菜分類、移除「999 件」文案；促銷標籤（本月限定／鍋物加料）改商品 tag 驅動；社會證明徽章（已售出 X 件＋口碑徽章，後台可逐商品隱藏或自訂文字：商品資料 > 一般）。

2.4 分類頁／首頁／行動版

特價商品「-XX%」折扣徽章（全站：分類卡片、首頁卡片、商品頁圖庫）；快速加購成功 toast（分類頁＋首頁）；首頁 Banner preload（LCP 優化）；行動版：44px 觸控目標、移除多餘放大鏡按鈕、排序標籤動態顯示實際預設排序。

2.5 上下快捷列（行動版）

上方：搜尋列點外部收合、Esc 關閉抽屜與搜尋、aria-expanded 與焦點管理、防空白搜尋。下方：圖示加文字標籤（首頁／收藏／購物車／會員）、iPhone 安全區內距、按壓縮放回饋、購物車徽章 99+ 上限。

2.6 我的帳號／登入頁

帳號控制台總覽儀表板（紅利點數卡＋近 3 筆訂單卡＋五個快速捷徑）；登入頁英文點數文案中文化（gettext＋JS 雙保險）；註冊表單「加入會員專屬好處」區塊。

2.7 出貨AI助理（後台 Agent）擴充

黑貓宅急便整合：託運單號填入（單筆對話式／批次貼上契客系統清單，自動寄客戶通知信）；貨態自動抓取回填（解析黑貓官網查詢頁，寫入訂單 meta 與備註）；後台訂單列表「黑貓託運單號」欄位（傳統＋HPOS）。快捷按鈕已加入助理頁。

2.8 分潤系統（自建，CyberBiz 顧客推薦模式）

第一階段點數軌：會員專屬推薦碼／連結、30 天 cookie 歸因（last-click）、自我推薦防護、訂單完成發點（小計 5%、上限 300 點）、被推薦人首購加贈 50 點、退款自動回收、我的帳號「推薦好友」專區（複製連結／LINE 分享／成效統計）。

第二階段現金軌：夥伴身分（KOL／團購主）與個別費率、前台申請＋後台審核、指定商品費率欄位、現金帳本（pending → 7 天鑑賞期 → confirmed → paid）、退款反沖、出金門檻 NT$1,000、扣繳 10%＋二代健保 2.11% 試算、QR Code 推廣工具。

後台「分潤夥伴管理」獨立頁：六張統計卡儀表板＋待審核申請／夥伴列表（行內調費率、一鍵出金、停用）／出金紀錄／近 30 天推薦訂單四個列表。AI 助理另支援對帳單、標記出金、核准夥伴等口語指令。

三、檔案結構與修改清單

四、部署流程

所有修改先改本機專案資料夾，再執行快速部署腳本上傳（僅上傳清單內檔案，不會動到其他遠端檔案）：

cd "/Volumes/Crucial X8/VIDEO/11507/ecom-wordpress" && python3 sftp_deploy_quick.py

目前部署清單（9 檔，2026-08-22 更新）：ecpay-ecpg-gateway.php、functions.php、front-page.php、header.php、ckc-referral.php、ckc-referral-partner.php、ckc-referral-admin.php、ckc-coupons.php、includes/woocommerce/checkout.php。新增主題檔案時記得同步加入清單，並同步更新第三章的檔案說明表。

⚠️ 重要教訓（2026-08-22）：includes/woocommerce/checkout.php 這支結帳頁核心檔案，先前並未列入部署清單，導致該檔案的任何本機修改實際上從未真正上線，卻沒有任何錯誤提示——本機檔案與正式站檔案長期不同步而不自知。詳見第十一章 11.2、11.3。強烈建議找時間逐一核對本機主題資料夾與正式站（可透過 SFTP 下載遠端檔案比對雜湊值）所有 .php 檔案，確認是否還有其他檔案同樣被漏掉、其修改從未真正部署。

注意：SFTP 帳號密碼以明文寫在 sftp_deploy.py／sftp_deploy_quick.py 內，請勿外流此資料夾；建議日後改用環境變數。完整主題部署可用 sftp_deploy.py（整個主題目錄遞迴上傳）——若懷疑清單有遺漏，此工具可作為交叉比對或緊急補救的手段。

常見操作失誤提醒：終端機（Terminal）路徑務必先確認在專案資料夾內（提示字元應顯示 ecom-wordpress，而非使用者家目錄 ~），否則 grep／sed 等指令會出現「No such file or directory」；PHP 程式碼片段不可直接貼到 Terminal 執行，會被 zsh 誤判為指令語法；多行 heredoc（python3 << 'EOF' ...）貼上長內容時容易被 Terminal 拆行導致語法錯誤，建議改用單行 python3 -c "..." 指令，或將腳本內容 base64 編碼後以單行 echo | base64 -d | python3 執行。

五、分潤系統操作手冊

5.1 日常操作（後台 > 分潤夥伴）

審核申請：待審核列表選擇 KOL／團購主、填費率（留空預設 8%）→ 核准。會員即在「我的帳號 > 推薦好友」看到現金儀表板與 QR Code。

出金：夥伴可出金額達 NT$1,000 時「標記出金」變為可按 → 先完成實際匯款 → 按下標記（系統顯示扣繳／二代健保／實付試算並寫入出金紀錄）。

一般會員（點數軌）無需管理：分享連結 → 好友下單完成 → 自動發點、退款自動回收。

5.2 AI 助理口語指令（出貨AI助理頁）

「請給我推薦分潤報表」／「產生夥伴分潤對帳單」／「標記會員 ID 123 的可出金分潤為已出金」／「核准會員 ID 123 成為推廣夥伴，費率 8%」（含「團購」字樣會歸類為團購主）。

5.3 可調參數（工程用 filter，改 functions.php 或子模組即可）

六、黑貓出貨操作手冊

填單筆：對 AI 助理說「將黑貓託運單號 9031234567890 填入訂單 #265」→ 寫入訂單並自動寄含查詢連結的通知信給客戶。

批次：從黑貓契客系統匯出「訂單號＋託運單號」清單，直接貼給助理（每行一組，最多 50 筆）。

貨態同步：「請自動抓取黑貓貨運狀態並回填後台」→ 最近 10 筆有單號的訂單逐一查詢黑貓官網並寫入貨態。訂單列表有「黑貓託運單號」欄可直接查看。

七、待辦事項與建議（依優先序）

✅ 已解決（2026-08-21）

原 🔴1：綠界金流「測試模式」問題 — 已排查並修正，正式收款前的最後障礙已排除。根本原因與修正內容詳見第十章 10.1～10.4。

✅ 已解決（2026-08-22）

結帳頁「顯示訂單明細」金額與實際總計不同步 — 已修正，摺疊列標題金額現在會隨 WooCommerce AJAX 重新計算即時更新。詳見第十一章 11.2、11.3。

部署清單漏掉 includes/woocommerce/checkout.php — 已補進 sftp_deploy_quick.py，該檔案的修改此後才會真正上線。詳見第十一章 11.2、11.3，並請留意下方新增待辦事項關於全面覆核的建議。

超商取貨選項完全無法選取 — 已排查並修正，根本原因是「綠界物流 超商取貨 7-ELEVEN」從未加入 WooCommerce 運送區域，與離島判斷邏輯無關；已補上運送方式並經多縣市（本島＋離島）實測驗證通過。詳見第十二章 12.1。

購物車頁免運進度橫幅偶爾停留在舊金額 — 已排查並修正，根本原因是 WooCommerce 核心 fragment 更新腳本在購物車頁未被載入，且改用主動 AJAX 抓取的第一版修法有競態問題；最終改為監聽購物車真正更新完成的事件才抓取最新內容，經連續多輪快速操作測試穩定通過。詳見第十二章 12.2。

結帳頁切換配送方式時地址欄位版面瞬間跳動 — 已修正，改用 slideUp／slideDown 平滑收合展開取代原本的瞬間 hide／show，並加上狀態判斷與動畫中斷保護，避免快速連續切換時動畫堆疊。經連續快速切換測試穩定通過。詳見第十三章 13.3。

✅ 已解決（2026-08-24）

信用卡付款元件初始化卡死、無限迴圈卡住瀏覽器主執行緒 — 已修正，ECPay.initialize() 與 renderECPay() 皆加上逾時／重試上限保護。詳見第十四章 14.1①、14.2.1。

自動自癒輪詢機制多次誤觸發、打斷進行中的付款請求 — 已整段移除，比對官方 API Skill 文件確認此非官方建議做法。詳見第十四章 14.1②、14.2.2、14.3。

診斷用 \n\n 換行字元造成整段內嵌 JavaScript 語法錯誤 — 已清除診斷程式碼，並建立「部署後以 new Function() 驗證語法」的標準流程。詳見第十四章 14.1③、14.2.3。

點選 LINE Pay 或超商代碼繳費，實際卻被導向 ATM 虛擬帳號 — 已修正，根本原因是三張付款卡片皆寫死導向已下架的舊外掛 mydybox_ecpay，改為對應目前真正啟用的官方閘道器。詳見第十四章 14.1④、14.2.4。

ConsumerInfo.MerchantMemberID 官方必填欄位缺失 — 已補齊，依登入會員 ID 或訪客 session 識別碼組成。詳見第十四章 14.1⑤、14.2.5。

付款逾時／驗證失敗訊息使用 alert() 有時被瀏覽器靜默阻擋 — 已改為畫面內嵌顯示錯誤訊息＋重試按鈕，不依賴瀏覽器彈窗機制。詳見第十四章 14.1⑥、14.2.6。

✅ 已解決（2026-08-30）

購物車頁「湊免運加購推薦」按鈕點擊看似無反應 — 已修正。根本原因並非功能失效（後端始終正常運作、商品確實有加入），而是成功後採用「等待 400 毫秒再整頁重載」的做法，加上 AJAX 本身耗時約 1.35 秒，使用者需等待近 1.8 秒畫面才變化，且重載會把捲動位置重置回頂端，因而誤判為沒有反應。已改為使用後端原本就回傳的 fragments 原地更新畫面並顯示成功提示，不再整頁重載。詳見第十五章。

待處理項目

八、已知限制

黑貓貨態抓取為解析官網 HTML（黑貓無公開 API）——官網改版時會退化為「查無貨態」提示人工查詢，不會寫入錯誤資料；託運單號的產生仍需在黑貓契客系統完成後匯入。

夥伴 QR Code 使用外部服務 api.qrserver.com 產生；若該服務停用，QR 圖示會消失但連結功能不受影響。

AI 助理採訊息意圖解析（正則）而非原生 function calling；新增指令需在 functions.php 的 ckc_ajax_gemini_chat 內加意圖區塊（現有 1a–1j 可作範本）。未設 Gemini API Key 時全部動作為沙盒模擬。

分潤帳本儲存在 user meta（適合目前量級）；若夥伴數成長至數百人或需複雜報表，建議依規劃文檔評估遷移至獨立資料表或商用外掛。

綠界站內付 2.0 自訂閘道（ecpay-ecpg-gateway.php）的 ReturnURL／OrderResultURL 是在每次呼叫 CreatePayment API 時，透過 WC()->api_request_url('chao_ecpg_callback') 與 WC()->api_request_url('chao_ecpg_result') 動態產生並直接傳給綠界，並非讀取綠界廠商專區「系統設定」頁面的成交頁面／失敗頁面／幕後回傳程式欄位。修改該三個欄位不會影響實際金流運作，詳見第十章。

九、相關文件索引

checkout_ux_optimization_plan.docx — 結帳頁優化規劃（第一階段已全部實作）

cart_ux_optimization_plan.docx — 購物車優化規劃（第一＋二階段已實作）

product_page_ux_optimization_plan.docx — 商品頁優化規劃（程式類已實作，內容類待辦）

affiliate_program_plan_taiwan.docx — 分潤制度規劃（第一＋二階段＋後台管理頁已實作）

十、更新記錄 — 2026-08-21：綠界正式環境「測試模式」問題排查與修正

背景：正式站結帳頁信用卡付款區塊持續顯示紅字「此為測試功能」，即使後台綠界外掛已確認取消勾選測試模式、商店代號／HashKey 也已填入畫面上顯示的值，問題仍未解決。以下記錄完整排查過程與根本原因，供日後參考。

10.1 根本原因（共兩處）

原因一：測試模式判斷式的預設值寫死為「yes」

includes/ecpay-ecpg-gateway.php 第 55 行原本邏輯：

$this->test_mode = (get_option('wooecpay_enabled_payment_stage', 'yes') === 'yes') || empty($mid) || $mid === '3312200';

問題：get_option() 的第二參數（找不到值時的預設值）寫死為 'yes'。經全站搜尋（含官方外掛目錄），確認 wooecpay_enabled_payment_stage 這個 option 從未被任何表單或程式寫入過，只有讀取。也就是說，無論在後台任何畫面怎麼勾選「啟用測試模式」，資料庫裡都沒有實際存值的機制，get_option() 永遠回傳預設值 'yes'，導致付款畫面永遠判定為測試模式。

原因二：防呆判斷式誤把正式商店代號當成測試碼

同一行判斷式中的 $mid === '3312200'，原本推測為攔截綠界公開測試特店編號的防呆機制。但經登入綠界廠商專區正式後台（系統設定 > 系統介接設定）確認，3312200 其實就是貴公司申請到的正式特店代號（綠界官方公開測試碼實際上是 2000132），這段防呆邏輯反而誤將自家正式帳號判定為測試碼，導致即使商店代號填寫正確，仍會被強制切換回測試模式。

10.2 修正內容

修正後的第 55 行（已部署上線）：

$this->test_mode = (get_option('wooecpay_enabled_payment_stage', 'no') === 'yes') || empty($mid);

get_option() 預設值改為 'no'，讓 option 不存在時預設判定為正式模式。

移除 $mid === '3312200' 這段誤判正式商店代號的防呆條件；保留 empty($mid) 防呆（商店代號空白時仍強制測試模式，避免誤上線）。

10.3 排查過程中確認的技術細節（供日後參考）

ReturnURL（幕後回傳程式）與 OrderResultURL（成交頁面）在本站的實作方式：$return_url = WC()->api_request_url('chao_ecpg_callback')；$order_result_url = WC()->api_request_url('chao_ecpg_result')。兩者為每次呼叫 CreatePayment API 時動態產生並直接傳給綠界，不會讀取綠界廠商專區「系統設定」頁面填的網址。

兩個端點皆已透過 add_action('woocommerce_api_chao_ecpg_callback', ...) 與 add_action('woocommerce_api_chao_ecpg_result', ...) 正確掛勾，實際處理邏輯在 WC_Gateway_Chao_ECPay_ECPg 類別的 handle_callback()（第 574 行起）與 handle_result()（第 613 行起）兩個方法內。

handle_callback() 內第 605 行已正確回應綠界規定格式 echo '1|OK';，符合官方「特店收到付款結果通知後須回應 1|OK」的要求。

綠界廠商專區「系統設定 > 信用卡交易設定」中的成交頁面／失敗頁面／幕後回傳程式三個欄位，雖對本站目前串接方式無實際作用，但建議填寫合理的值（避免與官方「不可將 ReturnURL 設成與 OrderResultURL 相同」的警告牴觸），已暫填為站內合理頁面（例如訂單頁／購物車頁／首頁），對應第七章待辦 🟡 8。

10.4 建議後續動作（金流部分）

正式上線收款前，務必進行一次小額真實刷卡測試（非模擬付款），確認訂單狀態能正確轉為已付款、且撥款流程正常。

若日後金流串接邏輯有調整，建議在程式碼中避免再出現「把特定商店代號寫死當測試碼」這類防呆條件，改用官方公告的測試碼（2000132）或改用其他明確旗標（例如環境變數／WP_ENV 常數）判斷測試／正式環境，降低誤判風險。

十一、更新記錄 — 2026-08-22：結帳頁配送方式排查與訂單明細金額同步問題修正

11.1 配送方式（超商／宅配／門市自取）被鎖住排查

背景：正式站結帳頁三張配送方式卡片（超商、宅配、門市自取）在部分情況下呈現淺色鎖住、無法點選，且無明確錯誤提示。排查過程中曾懷疑是離島地址判斷邏輯誤判（例如臺東縣、屏東縣），經逐行檢視 chao_gang_cheng_is_outlying_island_destination() 函式（採郵遞區號＋縣市名稱＋鄉鎮名稱三層比對，僅精準命中澎湖、金門、馬祖、綠島、蘭嶼、琉球）後排除此可能性——本島縣市不會被誤判為離島。

實際根本原因：WooCommerce 原生「運送區域」設定中，「台灣」區域僅掛載了三個原生運送方式（免費運送、單一費率、自行取貨），並未加入綠界物流外掛提供的「綠界物流 超商取貨 7-ELEVEN」方式。而前端判斷「超商」卡片是否可點選的邏輯，寫死只認 method_id 開頭為 Wooecpay_Logistic_CVS 的運送方式；後端 chao_gang_cheng_apply_shipping_management_rates()（運費管理頁面資料實際套用的函式）同樣預期存在此 method_id。由於這個運送方式從未被加入 WooCommerce 運送區域，無論收件地址為何，系統中都不存在對應的 rate，超商選項因此永遠無法選取。這與地址、離島判斷完全無關，是運送區域設定缺漏。

待辦：需至 WooCommerce > 設定 > 運送方式 > 運送區域 > 台灣（編輯），新增「綠界物流 超商取貨 7-ELEVEN」運送方式並啟用。若該選項在新增運送方式選單中不存在，需先確認綠界物流外掛（非本次排查的金流外掛，是另一支物流專用外掛）是否已安裝並啟用。此項對應第七章待辦事項，建議列為高優先，因為超商取貨是台灣電商常見的主要取貨方式。

宅配與門市自取有時也一併被鎖住的情況，排查後判斷與離島邏輯無關，程式碼註解顯示可能與 chao_gang_cheng_restrict_rates_by_shipping_class()（依商品「適用運送類別」交集允許的運送方式）有關，商品若未正確勾選運送類別，可能連帶影響宅配/自取可選性。此部分尚未完整排查，列入第七章待辦 🟠 10。

11.2 「顯示訂單明細」摺疊列金額與實際總計不同步

問題：結帳頁行動版有一個自訂的「顯示訂單明細」摺疊列（點擊展開/收合訂單明細），其標題旁邊顯示的金額，在購物車內容、運送方式或收件地址變動後（未重新整理頁面的情況下），會停留在頁面首次載入當下的舊金額，與展開後 WooCommerce 原生 order_review 區塊裡的真實總計不一致。

根本原因：includes/woocommerce/checkout.php 中的 initCollapsibleOrderSummary() 函式，在摺疊列首次建立時，把當下抓到的金額文字（totalText）直接寫死組進 HTML 字串一次性插入頁面；函式本身有 if ($('#chao-collapsible-summary-trigger').length > 0) return; 的防重複產生機制，之後不會再執行。而 WooCommerce 每次透過 AJAX 重新計算運費/總計完成後，只會更新原生 order_review 區塊內容，不會觸發這支函式重新執行，導致摺疊列標題的金額成為「凍結」的舊快照，未掛上任何同步更新機制。

修正內容：為金額顯示的 <div> 加上固定 id=

id="chao-collapsible-summary-total"

新增 updateCollapsibleOrderSummaryTotal() 函式，重新抓取當下正確金額並寫回該 id；並在既有的 WooCommerce updated_checkout / init_checkout 事件監聽（每次 AJAX 重新計算完成後觸發）中呼叫這支新函式，確保金額同步更新。已於 2026-08-22 部署上線並確認生效。

11.3 部署清單缺漏 includes/woocommerce/checkout.php（重要結構性問題）

在完成上述 11.2 的程式碼修正並執行 sftp_deploy_quick.py 部署後，發現該次部署清單僅上傳 8 個檔案，includes/woocommerce/checkout.php 並不在清單內——代表這支結帳頁核心檔案的本機修改，實際上從未真正上傳到正式站，卻沒有任何錯誤或警示訊息，本機與正式站版本已長期不同步而不自知。已於同日將此檔案補入 sftp_deploy_quick.py 部署清單（共 9 檔）並重新部署確認生效。

風險提示：這個「靜默漏傳」的狀況說明部署清單本身可能不完整，過去若有人針對 checkout.php 或其他未列入清單的檔案做過修改並自認已部署，該修改很可能從未真正上線。強烈建議找時間逐一核對本機主題資料夾與正式站遠端檔案（可透過 SFTP 或 sftp_deploy.py 全量比對）是否還有其他遺漏，詳見第七章待辦 🔴 9。

操作建議：Terminal 貼上長段程式碼時，避免使用多行 heredoc（python3 << 'EOF' ... EOF）語法，容易被貼上時的斷行處理搞亂導致語法錯誤；改用單行 python3 -c "..." 指令，或將腳本以 base64 編碼後用單行 echo '...' | base64 -d | python3 執行，可大幅降低複製貼上出錯機率。

11.4 建議後續動作（本次排查匯總）

優先把「綠界物流 超商取貨 7-ELEVEN」加入 WooCommerce 運送區域，這是目前唯一已知會讓超商選項完全失效的原因。

找時間全面核對本機與正式站檔案差異，排除是否還有其他檔案的修改從未真正部署（詳見 11.3）。

待超商運送方式補上後，重新完整測試一次三種配送方式在多個縣市（含本島與離島）下是否都能正常勾選，並確認運費金額與「運費管理」頁面設定一致。

十二、更新記錄 — 2026-08-22（下午）：超商運送方式修正驗證、免運橫幅競態問題排查

12.1 超商取貨運送方式修正後的多縣市實測驗證

背景：依 11.1 排查結論，將「綠界物流 超商取貨 7-ELEVEN」加入 WooCommerce 運送區域後，實際使用瀏覽器自動化工具在正式站逐一測試多種地址情境，確認修正是否真正生效、且沒有引入新的離島誤判問題。

超商取貨（一般情境）：可正常選取，正確顯示「7-11 冷凍取貨(先付款) NT$200」並要求選擇取貨門市。

屏東縣（本島）宅配：正確套用本島費率 NT$250，未被誤判為離島。

澎湖縣（真離島）：超商／宅配／門市自取三種配送方式皆可正常選取，宅配正確套用離島費率 NT$350，符合商家「離島也應能自由選三種配送方式」的既定規則。

臺東縣＋綠島鄉 對比 臺東縣＋臺東市：同一個縣、僅鄉鎮不同，前者正確套用離島費率 NT$350，後者正確套用本島費率 NT$250——證實離島判斷邏輯是精準比對到鄉鎮層級，不會誤判整個縣。

免運門檻（NT$2,000）：購物車金額達標後，結帳頁正確顯示「恭喜！您已達免運門檻」且運費歸零。

結論：原本排查並修正的「超商取貨從未加入運送區域」問題，經多縣市（本島＋離島）反覆實測全部通過，可視為已完整解決。

12.2 購物車頁免運進度橫幅偶發停留舊金額（競態問題）排查與修正

背景：測試過程中意外發現，購物車頁上方「還差 NT$X 即可享免運」進度橫幅，在用 +/- 按鈕變更商品數量後，有時不會即時更新，停留在舊金額；但側邊欄「購物車總計」與加購區塊的提示文字都能正確即時更新。這與先前修正的「結帳頁顯示訂單明細」問題外觀類似，但根本原因完全不同，排查過程一併記錄如下，供日後參考。

12.2.1 第一次嘗試：觸發 wc_fragment_refresh 事件（無效）

PHP 端已透過 add_filter('woocommerce_add_to_cart_fragments', ...) 正確註冊 .cart-shipping-progress-wrapper 這個區塊為 WooCommerce fragment，理論上只要觸發 wc_fragment_refresh 事件就能讓前端重新抓取。第一版修正在數量變更時加上這個觸發，但實測完全無效。

排查後發現：瀏覽器裡 wc_cart_fragments_params 這個全域變數不存在，代表 WooCommerce 核心負責處理 fragment 更新的 wc-cart-fragments 腳本，在購物車頁根本沒有被載入。進一步查證 functions.php，發現一段效能優化程式碼會依條件停用這支腳本，但條件式已正確排除購物車頁（is_cart() 應回傳 true），理論上不該被停用。多次確認後，此為 WordPress.com 平台層級的效能優化機制介入所致（無法從主題程式碼層面查證，非本主題邏輯寫死停用），並非本主題程式碼邏輯缺陷。

12.2.2 第二次嘗試：改用自行 AJAX 抓取＋手動解析替換（部分有效）

既然無法依賴 wc-cart-fragments 腳本，改用本主題其他地方已證實有效的手法（與單一商品頁「加入購物車」AJAX 攔截邏輯相同）：自行對目前頁面發送 AJAX 請求取得最新 HTML，用 jQuery 解析出目標區塊後手動替換。這個做法本身邏輯正確（用瀏覽器 console 手動觸發可穩定重現正確更新），但透過真實點擊 +/- 按鈕觸發時仍不穩定。

排查後確認：這是一個競態問題（race condition）。負責把數量真正寫回伺服器 session 的原生更新流程，本身也有自己的處理時間；本次修正用固定 600 毫秒計時器去抓最新頁面內容，時機抓得太早，經常在伺服器端真正完成更新之前就已發出請求，抓到的是舊資料。固定計時器的做法無法可靠地與「未知結束時間」的另一段非同步流程同步。

12.2.3 最終修正：改為事件驅動，監聽真正更新完成的時機

最終修正棄用固定計時器，改為監聽 WooCommerce 原生的 updated_cart_totals（購物車真正更新完成後才會觸發的事件）；只在這個事件觸發後才發送 AJAX 請求抓取最新內容，確保伺服器端資料一定已經是最新的，從根本上消除競態問題。同時對請求網址加上時間戳記參數並關閉快取（cache: false），避免瀏覽器自身的 HTTP 快取造成資料不同步。

修正後以真實瀏覽器操作連續測試三輪（單次調降、單次調升、連續快速點擊三次调降），橫幅金額皆能正確即時反映最新狀態，未再重現舊資料卡住的現象。

12.3 建議後續動作

本次排查過程中意外發現，這個主題有多處自訂購物車即時更新邏輯繞過 WooCommerce 核心 fragment 機制（因該機制在此站台環境下不可靠）；未來若要新增其他需要「購物車變動後即時更新」的畫面元素，建議直接沿用 12.2.3 的做法（監聽 updated_cart_totals 事件 + 帶時間戳記的 AJAX 重抓 + 手動替換），不要再依賴 wc_fragment_refresh／wc-cart-fragments。

wc-cart-fragments 腳本在購物車頁被平台層級機制攔截一事，目前僅止於觀察到現象、無法從主題程式碼確認根本原因，若日後有機會，建議請教 WordPress.com 支援團隊或查閱其效能優化功能文件，確認是否有官方排除清單機制可用，一勞永逸解決，而不必每個新功能都繞道自行實作。

十三、更新記錄 — 2026-08-22（晚間）：訪客結帳與會員優惠券體驗評測

背景：實際比較「不註冊訪客結帳」與「會員身份＋使用優惠券」兩種流程的網頁回饋效率，找出可再提升的體驗環節。以下記錄測試過程、發現、以及已完成的修正。

13.1 訪客結帳流程測試發現

優點：結帳頁 1→2→3→4 步驟指示清楚，並有 15 分鐘訂單保留倒數；聯絡電話欄位會即時過濾非數字字元；Email 欄位失焦後立即顯示格式錯誤提示且附範例；選擇縣市／鄉鎮後郵遞區號會自動正確帶出；「確認付款」按鈕直接顯示總金額，降低下單前的不確定感。

加入購物車成功提示——初次測試誤判為缺失，後修正結論：程式碼 showCartPopup() 機制運作正常，單一商品頁與分類頁快速加購兩種進入點皆會觸發置中彈窗（顯示「已加入購物車」）。初次測試因為手動截圖操作耗時，檢查時彈窗早已顯示完畢並消失，才誤判為沒有觸發，特此更正。另外，精準計時測試量到彈窗實際完全可見時間僅約 240 毫秒，比程式碼設計的 1800 毫秒短很多，原因尚未查明，但不影響「使用者有得到回饋」這個核心結論，可日後有餘裕再查。

優惠券輸入介面在訪客結帳全程都不可見——詳見 13.2。

13.2 優惠券可視性問題排查

購物車頁：程式碼裡確實存在一支專門給購物車頁用的優惠券面板函式 ckc_cart_coupon_center()，但其掛勾 add_action( 'woocommerce_before_cart', 'ckc_cart_coupon_center', 15 ) 整行被註解掉，且註解明確寫著「已由需求從購物車移除，僅保留於結帳頁面」——證實這是刻意的商業決策，不是程式缺陷。原生 WooCommerce 優惠券輸入框也因此被 CSS 一併隱藏，購物車頁目前對任何人（含會員）都不會顯示優惠券輸入介面。

結帳頁：另一支函式 ckc_checkout_coupon_panel()（掛在 woocommerce_before_checkout_form）會顯示「我的優惠券」面板與折扣碼輸入框，但函式第一行即 if ( ! is_user_logged_in() ) return;，僅登入會員可見；原生 WooCommerce 的預設優惠券輸入區塊也已被 remove_action 移除、且沒有登入判斷式（因為整個原生區塊都被拿掉了）。兩者相加的結果：訪客在結帳頁同樣完全看不到任何優惠券輸入管道。

這組成功／失敗訊息的前端處理機制（ckcApplyCoupon 與 updated_checkout 事件監聽）本身寫得相當完整：套用成功會呼叫 ckcApplySuccessUI() 顯示 toast，失敗則從 .woocommerce-NoticeGroup-updateOrderReview 解析出錯誤文字並用 ckcToast() 顯示——但這整套機制目前只有登入會員在結帳頁才看得到、用得到。

待決事項：訪客完全無法輸入優惠券代碼，是否符合商家目前「優惠券為會員專屬福利」的定位，需要商家確認方向後才能決定是否要另外開放訪客可見的輸入介面。此項已列入第七章待辦 🟠 11，尚未執行修改。

13.3 結帳頁配送方式切換造成地址欄位版面跳動——已修正

問題：結帳頁選擇「超商」或「門市自取」時，地址相關欄位（縣市、鄉鎮市區、詳細地址、郵遞區號）會透過 jQuery .hide() 瞬間完全消失；切回「宅配」時則用 .show() 瞬間出現。這會導致下方所有欄位的畫面位置在一瞬間整段跳動，如果使用者手速快、在跳動當下點擊下一個欄位，容易點空或點到版面重排後移過來的其他欄位，需要重新對焦、有時甚至需要重新輸入。

修正內容：將 includes/woocommerce/checkout.php 裡 syncUIStates() 函式中的 .hide()／.show() 改為 .slideUp(200)／.slideDown(200)，讓欄位以動畫方式平滑收合／展開，而非瞬間切換；同時加入目前可見狀態的判斷（僅在狀態真的需要改變時才觸發動畫）與 .stop(true, true) 動畫中斷保護，避免這支函式被高頻呼叫（例如每次 updated_checkout 事件）時animation 反覆疊加、卡住不動。

驗證：實際於瀏覽器連續快速切換「宅配→超商→宅配」三次，動畫與最終狀態皆正確、無堆疊卡住現象，確認修正穩定生效。

13.4 建議後續動作

優先請商家確認：優惠券是否應開放訪客結帳時輸入使用；若是，建議在結帳頁補上一個不限登入狀態即可見的優惠券輸入框，並沿用既有的 ckcApplyCoupon 前端邏輯與 toast 成功/失敗提示機制（該機制品質已經不錯，只需要移除登入限制或另外複製一份給訪客用）。

加入購物車彈窗實際顯示時間（約 240ms）與程式碼設計值（1800ms）不一致的原因，建議日後有餘裕時查明，不影響目前功能正確性，純屬錦上添花。

第十四章：信用卡付款（站內付 2.0）深度排查全紀錄 — 2026-08-24

本章記錄 2026-08-24 單日內針對 chao_ecpay_ecpg（綠界站內付 2.0 WebJS SDK）進行的完整排查與修正過程。這是本專案至今處理過最複雜、牽涉最多層根本原因的單一問題，過程中多次「以為修好了、實測又發現新的根本原因」，故完整保留排查脈絡，供後續接手者理解每個決策的來龍去脈，避免重複繞路。

14.1 問題鏈總覽（依發現順序）

以下每一項都是獨立、真實存在的根本原因，彼此環環相扣，任一項未修正都會導致付款失敗，因此逐一列出，方便日後快速定位：

① ECPay.initialize() 若卡住不回應（無成功無失敗回呼），isInitialized 旗標永遠停在 false，導致 renderECPay() 陷入每 100ms 呼叫自己一次的無限遞迴，把瀏覽器分頁主執行緒完全卡死——這是「信用卡輸入框彈不出來」最初症狀的根源。

② 「自動自癒機制」（每 500ms 檢查付款元件 DOM 是否為空、自動重新初始化）在使用者送出付款、等待 getPayToken() 回應期間，會誤判元件暫時性看起來是空的，把整個付款元件重新整組換掉，導致原本進行中的付款請求失去所依附的畫面元件、永遠等不到回應。

③ 為診斷②新增的 alert() 訊息裡包含 \\n\\n 換行字元，因外層以 PHP 雙引號字串包裹 wp_add_inline_script() 內容，PHP 會把 \\n 解讀成真正的換行字元、混入 JavaScript 單引號字串中，造成整段內嵌 <script> 語法錯誤、完全無法執行——包含①②的修正也因此一併失效。此為本次排查中最隱晦、最容易誤判方向的一個根本原因。

④ 「LINE Pay／虛擬 ATM 轉帳／超商代碼繳費」三張付款卡片，前端 JS 全部被寫死導向已經下架的舊外掛 mydybox_ecpay（商家已在後台手動移除此外掛），選取不到任何 radio、實際送出的是畫面上原本殘留選中的付款方式，導致不論點選 LINE Pay 或超商代碼繳費，最終都被誤導向 ATM 虛擬帳號頁面。

⑤ ConsumerInfo.MerchantMemberID 為綠界官方文件明確標註的必填欄位（格式：MerchantID_買家識別碼），原程式碼完全未帶此參數，可能影響綠界後台已啟用的「連續盜刷保護」等會員層級風控機制的準確性。

⑥ getPayToken() 逾時或驗證失敗時原本使用瀏覽器原生 alert() 顯示錯誤訊息，但因這是從非同步回呼觸發、非使用者直接手勢觸發的彈窗，Chrome 有時會靜默阻擋、不顯示任何視覺提示，導致使用者卡在「付款處理中」畫面卻看不到任何錯誤說明。

⑦ 拿到有效 PayToken、寫入隱藏欄位後、表單真正送出前的極短空檔，會被 WooCommerce 自身背景重新計算流程把整個付款區塊 DOM 重新渲染，連帶洗掉剛寫入的 PayToken 值。

⑧【尚未解決，詳見 14.9】使用者點擊「確認付款」按鈕的瞬間，WooCommerce 核心會觸發某種背景重新渲染機制，把已經渲染完成、使用者已填好資料的信用卡元件整個重建、打回初始「載入中」狀態，導致無論卡片資料多正確，付款都無法真正送出。已嘗試以旗標暫停 updated_checkout 觸發的重新初始化，實測仍未攔截成功，判斷觸發路徑可能不是透過 updated_checkout 這個自訂事件，需要更精準的即時斷點除錯才能定位。

14.2 逐項修正內容（程式碼層級）

14.2.1 初始化逾時與重試上限保護（對應 14.1 ①）

檔案：includes/ecpay-ecpg-gateway.php。ECPay.initialize() 呼叫加上 8 秒逾時計時器，逾時視同初始化失敗，設定 window.ChaoECPgInitFailed 旗標；renderECPay(token, retryCount) 加上參數，重試達 80 次（100ms × 80 = 8 秒）或偵測到初始化已確定失敗時，改為呼叫 showEcpayError() 顯示明確錯誤與「重新載入付款模組」按鈕，不再無限遞迴。

14.2.2 移除自動自癒輪詢機制（對應 14.1 ②，後於 14.2.5 進一步簡化整段移除）

原本的解法是幫 setInterval 加上「表單送出中（form.checkout 有 processing class）則暫停」的判斷；比對綠界官方 API Skill（ECPay/ECPay-API-Skill）文件後確認官方標準流程並無此類背景輪詢重建的建議做法，屬於專案自行增加的額外複雜度，且是多起問題的根源，故最終決定整段移除，僅保留頁面載入、切換付款方式、updated_checkout 事件三個正常觸發時機；若元件真的異常空白，改由使用者手動點擊既有的「重新載入付款模組」按鈕（showEcpayError() 內建）處理。

14.2.3 清除造成語法錯誤的診斷程式碼（對應 14.1 ③）

移除 alert() 內含 \\n\\n 的診斷用附加文字，以及送出表單前的一組 console.log 診斷區塊，恢復乾淨語法。此後每次修改前後均額外以瀏覽器 Console 執行 new Function(內嵌腳本全文) 驗證語法有效性，作為部署後的標準驗證步驟，避免同類問題再度發生。

14.2.4 修正付款卡片對應到正確的官方閘道器（對應 14.1 ④）

檔案：includes/woocommerce/checkout.php，.chao-payment-card 點擊事件處理。原本 credit／cod 以外的所有付款方式一律導向已下架的 mydybox_ecpay，改為依 data-payment 屬性分別導向目前真正啟用、存在於結帳頁 DOM 中的官方閘道器：

linepay → Wooecpay_Gateway_Twqr（綠界TWQR，涵蓋 LINE Pay 等多合一 QR 付款；需留意後台「綠界TWQR」啟用狀態曾有截圖顯示「停用中」與結帳頁 DOM 出現不一致的情況，商家應自行至 WooCommerce > 設定 > 付款 再次確認目前真實啟用狀態）

atm → Wooecpay_Gateway_Webatm（綠界網路ATM，與畫面文案「虛擬 ATM 轉帳」語意相符）

cvscode → Wooecpay_Gateway_Barcode（綠界超商條碼；測試當下 Wooecpay_Gateway_Cvs／綠界超商代碼 並未出現在結帳頁 DOM 中，推測可能有金額門檻限制，NT$100 測試訂單未達標準，此點尚待商家用較高金額訂單重新驗證是否會出現「超商代碼」選項，若商家希望兩種超商繳費並存，需另外處理）

14.2.5 補齊 MerchantMemberID 必填欄位（對應 14.1 ⑤）

ajax_get_token() 方法中，於呼叫 GetTokenbyTrade 之前，依登入狀態組出識別碼：已登入會員使用 u+WordPress使用者ID（例如 u123），訪客則使用 g+WooCommerce session 客戶識別碼雜湊前 12 碼（例如 g3f9a2c1b8e4）；最終組合為 {MerchantID}_{識別碼} 寫入 ConsumerInfo.MerchantMemberID。此欄位呼叫時機在訂單建立之前（GetTokenbyTrade 於下單前即會呼叫），故無法使用 order_id，改用上述識別碼方案。

14.2.6 錯誤與逾時提示改為畫面內嵌顯示（對應 14.1 ⑥）

getPayToken() 的 errMsg 分支與 15 秒逾時分支，皆從 alert() 改為呼叫既有的 showEcpayError()，直接將訊息文字與「重新載入付款模組」按鈕寫入 #ecpay-loading 區塊，不依賴瀏覽器原生彈窗機制，確保使用者一定看得到提示，不會再出現「Console 有錯誤訊息、畫面卻毫無反應」的落差。錯誤文字並附上 SDK 回傳的原始 errMsg 內容（例如持卡人姓名格式錯誤的完整說明），方便使用者自行排除輸入問題。

14.2.7 PayToken 防禦性重新賦值（對應 14.1 ⑦）

getPayToken() 成功回呼中，改為：先以 JS 變數（非僅 DOM 欄位）保存 PayToken；以每 50ms 執行、共 4 次的短時間計時器持續重新賦值該隱藏欄位；250ms 後改用原生 DOM 方法 formEl.submit()（而非 jQuery 的 form.submit()，因後者實際上是觸發 submit 事件、可能被表單上其他已綁定的 submit 事件監聽器攔截）送出表單，降低送出前空檔值被外部重新渲染洗掉的機率。

14.3 官方 API Skill 比對結果

商家於本次排查過程中安裝了綠界官方發布的 AI 開發輔助套件（ECPay/ECPay-API-Skill，https://github.com/ECPay/ECPay-API-Skill），本文件作者據此逐項比對 chao_ecpay_ecpg 的實作內容：

SDK 網址依測試／正式環境正確切換（正式環境使用 ecpg.ecpay.com.tw，非 stage 版）——符合規範

GetTokenbyTrade／CreatePayment API 端點正確——符合規範

回應驗證順序：先驗證 TransCode===1（傳輸層），再驗證 RtnCode===1（業務層）——符合規範

<div id="ECPayPayment">：SDK 硬編碼、未更改——符合規範

ConsumerInfo.MerchantMemberID 必填欄位——原本缺失，已於 14.2.5 補齊

MerchantID／HashKey／HashIV 儲存方式——測試模式寫死綠界公開測試碼（無安全疑慮），正式模式透過 get_option() 從資料庫讀取，非硬編碼於原始碼——符合官方「禁止寫在程式碼中」的精神

上線前檢查清單（ReturnURL／OrderResultURL 為正式網址、Callback 走 HTTPS）——已確認符合

官方文件未見任何「背景輪詢檢查 DOM、自動重新初始化」的建議做法，佐證 14.1 ②的自癒機制屬於專案自行增加的非標準額外複雜度

14.4 尚未解決的問題：付款送出瞬間元件被重置

這是本次排查最終未能解決的核心問題。現象：使用者填妥卡號、有效期限、安全碼、持卡人姓名（含正確英文格式）後點擊「確認付款」，信用卡輸入元件會被整個重新初始化、打回「載入安全金流模組中」的最初狀態，隱藏欄位 PayToken 被清空，且無論等待多久都不會恢復，也不會有任何逾時或錯誤提示（因為根本沒有進入我們自己的 getPayToken 流程，是在更前面的階段就被打斷）。

已嘗試的修正：加上 window.chaoPlaceOrderClicked 旗標，於 #place_order 按鈕的 click 事件中設為 true 並於 8 秒後自動重置，updated_checkout 事件監聽器內檢查此旗標為 true 時跳過重新初始化。實測部署後，同一現象依然發生，且直接檢查旗標狀態顯示為 false——代表觸發這次重新渲染的路徑，很可能不是透過我們監聽的 updated_checkout 這個自訂 jQuery 事件，而是 WooCommerce 核心在「Place Order」按鈕點擊處理鏈中，透過其他內部機制（可能是直接的 AJAX 回呼、或不同的事件/流程）直接置換了付款區塊 HTML，繞過了目前所有已知的攔截點。

建議下一步：由具備即時瀏覽器除錯能力的工程師，在 Chrome DevTools 的 Sources 分頁對 checkout_place_order_chao_ecpay_ecpg 事件監聽器起始處、以及 WooCommerce 核心 checkout.js 中觸發 payment_fields 重新渲染的相關函式（可搜尋 update_checkout 或 updateOrderReview 關鍵字）設中斷點，實際點擊「確認付款」、逐步執行（step through），即可在數分鐘內精準定位是哪一段程式碼、在哪個時間點觸發了這次重建，比透過遠端截圖與貼指令來回的方式快速許多。此為本文件撰寫當下唯一建議的下一步。

14.5 測試過程中意外發現：真實網路中斷

排查過程中曾出現一次「卡在付款處理中超過一分鐘」的情況，經檢查瀏覽器 Console 發現當下所有對外連線（包含綠界 API、Google Analytics、WordPress.com、PayPal 等完全無關的第三方服務）同時出現 net::ERR_INTERNET_DISCONNECTED，證實是使用者裝置當下真實斷網，並非程式碼問題。此案例顯示：日後遇到類似「卡住不動」的回報，建議第一時間先請使用者檢查 Console 是否有此類瀏覽器層級的網路中斷訊號，可快速排除是否為環境因素、避免誤判為程式碼缺陷。

14.6 本次排查採用的技術方法（供日後參考）

影片畫格分析：使用者提供螢幕錄影時，以 ffmpeg 抽取每秒 2 張畫格逐一檢視，定位錯誤訊息與畫面狀態變化的確切時間點。

heredoc + sed 行號替換：修改 PHP/JS 程式碼時，先將新內容寫入暫存檔，再以 sed 精確刪除舊行號範圍、插入新內容，避免長段程式碼直接貼入終端機時因跳脫字元或換行被截斷而出錯；每次修改後以 php -l 驗證 PHP 語法、並在瀏覽器以 new Function(內嵌腳本全文) 額外驗證 JavaScript 語法。

即時瀏覽器監控：透過 Claude in Chrome 連線，於使用者填寫表單、點擊按鈕的當下同步監看 Network 與 Console 面板，比純粹依賴使用者事後截圖更能掌握精確的時間序列與競態情況。

官方文件比對：安裝綠界官方 AI Skill 套件（ECPay-API-Skill）後，逐項對照官方規範文件，用以區分「專案自訂邏輯造成的問題」與「未符合官方規範的合規缺口」兩類不同性質的問題，避免混為一談。

第十五章：購物車加購按鈕「點擊無反應」修正 — 2026-08-30

本章記錄購物車頁「湊免運加購推薦」區塊按鈕的問題排查與修正。與第十四章的信用卡問題不同，這次的根本原因單純明確，且排查方式改為由 AI 直接透過瀏覽器連線即時操作與量測，未經由使用者手動貼指令，效率大幅提升，此方法建議日後沿用。

15.1 使用者回報症狀

購物車頁未達免運門檻時顯示的「還差 NT$X 免運，加購這些剛剛好」推薦區塊，其「加入購物車」按鈕點擊後看似沒有任何反應：購物車內容與免運差額金額都沒有變化，使用者因此重複點擊或放棄操作。

15.2 排查過程與實測數據

透過瀏覽器直接連線正式站，逐層驗證後確認：

後端完全正常：直接呼叫 AJAX action ckc_add_addon_to_cart，回傳 success:true、正確訊息（例如「已成功加購『黃金泡菜｜400g/瓶』！」），商品確實有被寫入購物車。

前端事件綁定正常：.chao-cross-sell-add 在 document 上有正確的委派 click 監聽器，點擊後按鈕文字確實變為「加入中...」，AJAX 也確實送出。

實測 AJAX 本身耗時約 1,350 毫秒（1.35 秒），屬於偏慢但仍在正常範圍。

成功回呼中使用 setTimeout(function(){ window.location.reload(); }, 400) 做整頁重新載入。

兩者相加，使用者從點擊到畫面出現任何變化需等待約 1.75 秒；期間唯一的視覺回饋只有按鈕文字變成「加入中...」，非常不明顯。

整頁重載完成後，捲動位置被重置回頁面頂端，使用者被「彈」到畫面最上方，更難察覺購物車其實已經更新，因而誤判為完全沒有反應。

後端回傳的 data 中原本就包含 fragments 與 cart_hash（WooCommerce 專為「原地更新畫面、不需整頁重載」設計的資料），但前端程式碼完全沒有使用，反而選擇了最粗暴的整頁重載。

重要佐證：使用者提供的兩段螢幕錄影可互相印證此結論——第一段影片中點擊後看似毫無反應，但第二段影片一開始，購物車內就已經存在第一段影片所加購的商品。這證實商品自始至終都有正確加入，問題純粹出在畫面回饋，而非功能失效。

15.3 修正內容

修改檔案涉及購物車加購相關的前端邏輯（.chao-cross-sell-add 點擊處理器與彈窗送出路徑 #ckc-addon-modal-submit），修正要點如下：

移除 400 毫秒的 setTimeout 與 window.location.reload() 整頁重載。

改用後端回傳的 fragments 原地更新購物車表格、免運進度條、右上角購物車徽章與下拉選單。

成功後立即呼叫 showToast(res.data.message || '已成功加入購物車！') 顯示成功提示，讓使用者第一時間得到明確回饋。

同樣的處理一併套用到有規格商品的彈窗送出路徑 #ckc-addon-modal-submit。

依照第十二章 12.3 的既有結論，未使用 wc_fragment_refresh／wc-cart-fragments（該腳本在此站台被 WordPress.com 平台層級機制攔截而不可靠），沿用主題既有的手動替換手法。

15.4 部署後實測驗證結果

部署後以瀏覽器實際點擊驗證，各項指標如下：

整頁重載已消除：於點擊前在頁面注入標記變數，點擊後標記仍存活，證實頁面未重新載入。

捲動位置保持不變：點擊前後 window.scrollY 皆為 1364，使用者不再被彈回頁面頂端。

購物車即時更新：商品列數由 2 項增為 3 項，原地更新無需重載。

免運進度同步：差額由「還差 NT$631」正確更新為「還差 NT$431」（加購商品為 NT$200）。

總計金額同步：NT$869 → NT$1,069，計算正確。

右上角購物車數字徽章與下拉選單內容一併同步更新。

程式碼層面確認：線上版本的處理器中 location.reload 已不存在，且確實引用 fragments 與 cart_hash。

唯一未能完全確認的項目：成功 toast 的實際渲染。已於程式碼層面確認成功回呼中有正確呼叫 showToast()，但自動化偵測腳本未能捕捉到其渲染出的 DOM 元素（可能因顯示時間極短，或偵測方式未涵蓋其實作方式）。建議由人工實際點擊一次以肉眼確認提示有正常顯示，此項列入第七章待辦 🟢 15。

15.5 本次採用的排查方法（建議日後沿用）

本次排查全程由 AI 透過瀏覽器連線直接操作正式站完成，全程未需使用者手動於終端機貼上任何診斷指令，效率明顯優於先前「使用者操作→截圖→AI 判讀→再指示」的來回模式。具體手法包括：

直接讀取事件處理器原始碼：以 jQuery._data(document, 'events') 取出委派事件的 handler，再用 handler.toString() 讀取其實際邏輯，可在完全不接觸伺服器檔案的情況下確認前端程式碼行為。

攔截 jQuery.ajax 觀察實際送出的參數與 action 名稱，確認前端是否真的發出請求、以及送出了哪些資料。

直接呼叫後端 AJAX endpoint 並檢視完整回傳結構，藉此明確區分「後端故障」與「前端未正確處理回應」兩種完全不同的問題。

以注入頁面標記變數的方式判斷頁面是否發生重新載入，比觀察畫面更精確可靠。

量測實際耗時（AJAX 往返時間、各階段延遲），以客觀數據取代主觀感受判斷問題性質。

15.6 作業過程中的失誤紀錄（供警惕）

排查期間為測試而往購物車加入了多項商品，事後清理時所使用的自動化腳本，在解析商品名稱時取得空字串，導致「保留原有商品」的判斷條件失效，誤將使用者原本的 2 項商品一併刪除，等同清空整個購物車。事後已查出兩項商品的 ID（347 與 483）並連同規格（口味：藤椒醬）完整還原，金額與原狀態一致，未造成實質損失。

教訓：任何具有刪除性質的批次腳本，務必在執行刪除前先驗證「用於判斷保留與否的關鍵欄位」確實有正確取值，若取值為空或異常應立即中止而非繼續執行。此原則同樣適用於日後任何涉及訂單、商品、會員資料的批次處理作業。

第十六章：同溫層結帳限制 — 規格與決策紀錄（2026-08-30，待實作）

⚠️ 本章記錄的是【尚未實作】的功能規格與其決策過程，不是已上線的變更。完整規格書為專案根目錄的 spec-同溫層限制與運費顯示修正.md，本章記錄的是「為什麼最後這樣決定」，供日後接手者理解脈絡，避免重蹈評估過程。

16.1 需求背景

商家反映：目前結帳頁可以同時包含不同溫層（常溫／冷藏／冷凍）的商品並完成下單，但物流實際上無法混溫層出貨，因此這類訂單倉庫根本做不到，屬於「系統允許、實務無法履行」的訂單，必須從系統面阻擋。

16.2 排查中的重大發現：運費管理早已支援溫層

排查現況時發現後台存在自訂管理頁「運費管理」（admin.php?page=ckc-shipping-management），其資料結構為 ckc_shipping[配送方式][地區][溫層][級距]，也就是說「配送方式 × 地區 × 溫層」的三維結構早在當初開發時就已建好並填入設定值：

免運門檻（ckc_shipping_free）：宅配 NT$1,500、超商 NT$1,500。

運費：宅配／超商 本島皆 250、離島皆 350（三個溫層目前設為相同值）；門市自取 0。

經瀏覽器實測確認，後端運費計算確實有讀取這份設定：小計 1,568（≥1,500）顯示免運、小計 1,368 於台中市為 250、於澎湖縣為 350，離島加價亦正常生效。

16.3 同時發現的既有 Bug：站內免運門檻顯示三處不一致

後端計算雖正確，但前端顯示存在明確錯誤，且站內三處說法不同：

頁面頂端公告：顯示「滿 $1,500 免運」——正確。

購物車免運進度條：顯示「免運門檻 NT$2,000」——錯誤，應為 1,500，疑似寫死於程式碼中未讀取設定。

購物車預估運費：顯示「宅配 250｜超商 250」——不完整，未反映離島 350 的差異。

影響：客人實際已達 1,500 免運資格，畫面卻顯示還需消費到 2,000，等同誤導消費者，且與結帳頁實際計算結果不符，屬客訴與信任風險。此問題已納入規格書第二階段處理。

16.4 方案評估與最終決策（重要，請務必閱讀）

由於發現運費管理早已具備溫層維度的設定，當下曾評估兩個方向：

方案 A（同溫層限制）：禁止混溫層購物車，一筆訂單只能有一種溫層。優點是出貨邏輯單純；缺點是客人想同時買常溫禮盒與冷凍年菜必須分兩次下單、付兩次運費，棄單率可能上升。

方案 B（拆單計費）：允許混買，依溫層拆成多個 WooCommerce package，各自計費、各自判斷免運門檻。優點是不擋任何訂單、客單價可往上疊；缺點是運費計算與後台出貨流程較複雜，且需在結帳前清楚揭露「本單將分 N 箱出貨」以免客訴。

商家一度選擇方案 B，並已據此完成一版拆單規格書（含免運門檻各箱獨立計算、每箱 NT$1,500、超商三溫層皆可收、離島加價確認等決策）。但隨後於同日改為最終決議：【永久採用方案 A，不做拆單】。

給日後接手者的提醒：後台運費管理保有完整的「溫層 × 地區 × 配送方式」三維設定，很容易讓人誤以為系統本來就要支援混溫層拆單出貨。實際上並非如此——這是商家經評估後主動否決的方向。在方案 A 下購物車必定為單一溫層，該三維結構仍然有用（用來取得該溫層對應的費率），但不會出現一筆訂單多箱計費的情況。

16.5 溫層判定規則摘要

溫層依商品分類（product_cat）判定，非 WooCommerce 運送類別。特別注意：WooCommerce 原生的「運送類別」在本站為 宅配／自取／超商自取／離島（屬配送方式而非溫層），且四個類別皆掛 0 個商品、等同未使用，實作時勿誤用。

常溫（ambient）：warmgift（常溫禮盒）、warmfood（常溫食品）

冷藏（chilled）：side-dishes（冷藏佳餚）

冷凍（frozen）：icegift（冷凍禮盒）、frozen（冷凍美食）

allitem（全部商品）：所有商品皆掛，屬導覽用途，判定溫層時必須忽略。

combo（合併下單區）：定位為 universal（溫層通用搭配商品），常溫可存放，可與任何溫層一起購買。

核心相容性規則：一台購物車只能有一種溫層；universal 商品是萬用牌，可與任何溫層並存，但自身不會決定購物車的溫層。購物車的「當前溫層」取自車內第一個非 universal 商品；若全車皆為 universal，則視為尚未鎖定溫層，任何商品皆可加入。

16.6 「合併下單區」定位的轉變

此分類原始用途為「讓客人跨溫層湊單」。在方案 A 下，跨溫層本身已被禁止，該用途不再成立。規格書中已將其重新定義為「溫層通用的搭配商品」，用途轉為協助客人把【當前這個溫層】的金額推向免運門檻，因此仍具價值。

此定義為規格撰寫時的推論，尚待商家最終確認；若商家對該分類另有規劃（例如調整用途或下架），需回頭修正規格。此項已列入第七章待辦 🟡 17。

16.7 實作範圍摘要（詳見規格書）

規格書將實作分為三階段：

第一階段（最高優先，先止血）：阻擋混溫層。含後端強制驗證（woocommerce_add_to_cart_validation 與結帳前再驗證）、前端即時攔截彈窗（需說明原因並提供「清空購物車改買此商品」的出口）、既有混溫層 session 的引導處理（不可靜默清空客人購物車）、以及加購推薦清單必須連動過濾為相容溫層。

第二階段：修正前端運費顯示，一律改為讀取運費管理設定，消除 2,000 與 1,500 不一致的問題。驗收方式為後台改門檻後前端應自動同步，無需改程式碼。

第三階段：溫層資訊呈現優化，於商品頁、列表頁、購物車頁標示溫層，讓客人在加入購物車前就知道，降低撞到限制的機率。

特別提醒：所有加入購物車的入口都必須驗證，包含商品頁、分類頁快速加購、購物車頁的「湊免運加購推薦」（AJAX action ckc_add_addon_to_cart）、以及結帳頁加購區塊。其中加購推薦最容易被遺漏。

16.8 待釐清事項（實作前需先讀程式碼確認）

目前運費計算是否真的有依溫層取費率？因三個溫層費率目前都設成相同值（250／350），從畫面上無法判斷。在方案 A 下購物車必為單一溫層，影響較小，但仍需確認取值正確，否則將來三溫層設不同價時會算錯。

chao_gang_cheng_restrict_rates_by_shipping_class() 是否仍有作用？此函式依 WooCommerce 運送類別運作，但運送類別掛 0 個商品，可能已成 dead code。第七章待辦 🟢 10 記載的「宅配／門市自取偶爾被鎖住」疑慮疑似與此有關，可一併釐清。